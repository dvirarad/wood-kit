{
  "name": "wood-kits-platform",
  "version": "1.0.0",
  "description": "Complete Wood Kits e-commerce platform with React frontend and Node.js backend",
  "scripts": {
    "start": "echo '🚀 Use npm run start:backend and npm run start:frontend in separate terminals'",
    "start:backend": "cd backend && npm start",
    "start:frontend": "cd frontend && npm start",
    "seed": "cd backend && npm run seed",
    "verify": "node verify-system.js",
    "test:api": "cd backend && node test-api.js",
    "dev": "concurrently \"npm run start:backend\" \"npm run start:frontend\"",
    "install:all": "npm install && cd backend && npm install && cd ../frontend && npm install",
    "clean": "rm -rf node_modules backend/node_modules frontend/node_modules",
    "build": "cd frontend && npm run build",
    "test": "cd backend && npm test && cd ../frontend && npm test",
    "test:backend": "jest",
    "test:e2e": "playwright test",
    "test:e2e:ui": "playwright test --ui",
    "test:coverage": "jest --coverage",
    "test:all": "npm run test:backend && npm run test:e2e",
    "install:test-deps": "npm install --save-dev jest supertest mongodb-memory-server @playwright/test"
  },
  "keywords": [
    "ecommerce",
    "woodworking",
    "react",
    "nodejs",
    "mongodb",
    "multilingual"
  ],
  "author": "Wood Kits Team",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=8.0.0"
  },
  "dependencies": {
    "jest": "^30.0.5",
    "supertest": "^7.1.4"
  }
}
